# Build / CI
CI=false
GENERATE_SOURCEMAP=false

# Avoid ESLint "Plugin react was conflicted" (path casing: scheduler vs Scheduler).
# Set to false and open/run the project with consistent path casing if you want build-time linting.
DISABLE_ESLINT_PLUGIN=true
